# lvalue, rvalue, lvalue references and an intro to rvalue references

- **lvalue** : something with a memory location (ex: `&(i);)
- **rvalue** : (on the right side of assigement) does not point anywhere
- **lvalue_reference**: `int& lvalue = i;` <- basically a alias
- **rvalue_reference**