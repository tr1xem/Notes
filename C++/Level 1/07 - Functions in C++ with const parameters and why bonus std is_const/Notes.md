# Functions in C++ with const parameters and why (bonus std::is_const)

std::is_const just checks if its const or not 

![](assets/Pasted%20image%2020260213153206.png)

std::is_const_v is inline version